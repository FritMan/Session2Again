<Page x:Class="Session2Try2.Pages.ClientBillPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Session2Try2.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="ClientBillPage">

    <ScrollViewer>
        <StackPanel>
            <StackPanel x:Name="BillSp">

                    <GroupBox Header="ФИО">
                    <StackPanel Orientation="Vertical">
                        <ComboBox x:Name="ClientCb" SelectedItem="{Binding Client}" SelectionChanged="ClientCb_SelectionChanged">
                            <ComboBox.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Vertical">
                                        <TextBlock Text="{Binding Fio}"/>
                                    </StackPanel>
                                </DataTemplate>
                            </ComboBox.ItemTemplate>
                        </ComboBox>
                        <TextBox x:Name="PhoneTb" Text="{Binding Phone}" IsReadOnly="True"/>
                    </StackPanel>
                </GroupBox>

                <GroupBox Header="Период оплаты">
                    <StackPanel Orientation="Horizontal">
                        <Label Content="С"/>
                        <DatePicker Name="StartDp" SelectedDateChanged="StartDp_SelectedDateChanged"/>
                        <Label Content="по"/>
                        <DatePicker Name="EndDp" SelectedDateChanged="EndDp_SelectedDateChanged"/>
                    </StackPanel>
                </GroupBox>


                <DataGrid x:Name="ServiceDg" AutoGenerateColumns="False" IsReadOnly="True">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Название" Binding="{Binding Service.Name}"/>
                        <DataGridTextColumn Header="Цена" Binding="{Binding Service.Price}"/>
                    </DataGrid.Columns>
                </DataGrid>

                <GroupBox Header="Финальная сумма">
                    <Label x:Name="TotalLab" Content="0"/>
                </GroupBox>
                
               
            </StackPanel>
            <Button x:Name="PdfBtn" Click="PdfBtn_Click" Content="PDF"/>
            <Button x:Name="CsvBtn" Click="CsvBtn_Click" Content="CSV"/>
        </StackPanel>
    </ScrollViewer>
</Page>
